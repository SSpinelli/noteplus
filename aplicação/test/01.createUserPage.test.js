const expect = require('chai').expect;
// const resolution = require('../src/pages/CreateUser')

const foo = 2;
const bar = 2;

describe('sjdiasjdiasj', () => {
  it('sdds', () => {
    expect(foo).equal(bar)
  });
});
